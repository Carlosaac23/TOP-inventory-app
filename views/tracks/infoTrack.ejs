<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= track.model %> - Track Details</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <%- include('../partials/header.ejs') %>

  <main class="container">
    <div class="page-header">
      <h1 class="page-title">🛤️ <%= track.model %></h1>
      <p class="page-subtitle">Model #<%= track.model_id %></p>
    </div>

    <article class="product-card">
      <div class="product-image">
        <img class="info-image" src="<%= track.image_url %>" alt="<%= track.model %> scale model">
        <%- include('../partials/stock-span.ejs', { item: track }) %>
      </div>

      <div class="product-content">
        <div class="product-category">Track</div>
        <div class="product-btns">
          <h2 class="product-name"><%= track.model %></h2>
          <button class="btn btn-secondary">
            <a href="<%= track.id %>/update" style="text-decoration: none; color: inherit">Edit</a>
          </button>
        </div>
        <p class="product-model">Model #<%= track.model_id %></p>
        <p class="product-description"><%= track.description %></p>

        <%- include('../partials/meta-items.ejs', { item: track }) %>

        <div class="product-footer">
          <span class="product-price">$<%= track.price %></span>
          <span class="product-stock"><%= track.stock_quantity %> available</span>
        </div>

        <div style="margin-top: 1.5rem; display: flex; gap: 1rem; flex-wrap: wrap;">
          <a class="btn btn-primary" href="/tracks">Back to Tracks</a>
          <a class="btn btn-outline" href="/">Return Home</a>
        </div>
      </div>
    </article>
  </main>

  <%- include('../partials/footer.ejs') %>
</body>
</html>
