<div>
  <label class="filter-label" for="category">Categories:</label>
  <select id="category">
    <option value="">All Categories</option>
    <% categories.forEach(category => { %> 
      <option value="<%= category.id %>"><%= category.name %></option>
    <% }); %>
  </select>
</div>